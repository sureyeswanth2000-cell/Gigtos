// LEGACY COMPATIBILITY NOTE:
// `SocioScore` was the old name. New product/code should say `GigScore`.
// Keep this re-export until all imports/tests are migrated, then remove in cleanup.
export * from './gigScore';
